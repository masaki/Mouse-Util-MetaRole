This is Perl module Mouse::Util::MetaRole.

INSTALLATION

Mouse::Util::MetaRole installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Mouse::Util::MetaRole

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Mouse::Util::MetaRole documentation is available as in POD. So you can do:

    % perldoc Mouse::Util::MetaRole

to read the documentation online with your favorite pager.

NAKAGAWA Masaki
